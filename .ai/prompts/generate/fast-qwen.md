# Qwen Prompt: Fast Component Generation

Generate a UI component based on the user request.

## Constraints
- Use semantic HTML5.
- Styling: Use Tailwind CSS ONLY if requested, otherwise use modern Vanilla CSS.
- Ensure responsiveness (mobile-first).
- Clean and minimal code.

## Rules
- No external dependencies unless specified.
- No explanatory text, just code.
- Follow `.ai/memory/ai-behavior.md`.
